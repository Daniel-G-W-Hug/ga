// AUTO-GENERATED BY ga_bindgen — DO NOT EDIT.
// Regenerate via: python3 ga_bindgen/src/emit_nanobind.py --all
// Source manifest: ga_bindgen/manifest.json

#include <nanobind/nanobind.h>
#include <nanobind/ndarray.h>
#include <nanobind/operators.h>
#include <nanobind/stl/array.h>
#include <nanobind/stl/pair.h>
#include <nanobind/stl/string.h>
#include <nanobind/stl/string_view.h>
#include <nanobind/stl/vector.h>
#include <fmt/format.h>
#include <array>
#include <stdexcept>
#include <string>
#include <string_view>
#include <utility>
#include <vector>

#include "ga/ga_ega.hpp"
#include "ga/ga_pga.hpp"

namespace nb = nanobind;
using namespace hd::ga;
using namespace hd::ga::ega;
using namespace hd::ga::pga;

void bind_trivec4d(nb::module_& m) {
    nb::class_<trivec4d>(m, "trivec4d")
        .def(nb::init<>())
        .def(nb::init<double, double, double, double>())
        .def("__init__",
            [](trivec4d* self,
               nb::ndarray<double, nb::shape<4>, nb::c_contig,
                           nb::device::cpu> arr) {
                double const* d = arr.data();
                new (self) trivec4d(d[0], d[1], d[2], d[3]);
            })
        .def_rw("x", &trivec4d::x)
        .def_rw("y", &trivec4d::y)
        .def_rw("z", &trivec4d::z)
        .def_rw("w", &trivec4d::w)
        .def("__repr__", [](const trivec4d& v) {
            return fmt::format("trivec4d({}, {}, {}, {})", v.x, v.y, v.z, v.w);
        })
        .def("__str__", [](const trivec4d& v) {
            return fmt::format("{}", v);
        })
        .def("__format__",
            [](const trivec4d& v, std::string_view spec) {
                try {
                    if (spec.empty()) return fmt::format("{}", v);
                    return fmt::format(fmt::runtime("{:" + std::string(spec) + "}"),
                                       v);
                } catch (fmt::format_error const& e) {
                    throw std::invalid_argument(e.what());
                }
            }, nb::arg("format_spec"))
        .def("__array__",
            [](trivec4d const& v, nb::handle /*dtype*/,
               nb::handle /*copy*/) {
                auto* data = new double[4]{v.x, v.y, v.z, v.w};
                nb::capsule owner(data, [](void* p) noexcept {
                    delete[] static_cast<double*>(p);
                });
                return nb::ndarray<nb::numpy, double, nb::shape<4>>(
                    data, { 4 }, owner);
            },
            nb::arg("dtype").none() = nb::none(),
            nb::arg("copy").none() = nb::none())
        .def(-nb::self)
        .def(nb::self + nb::self)
        .def(nb::self - nb::self)
        .def(nb::self += nb::self)
        .def(nb::self -= nb::self)
        .def(nb::self == nb::self)
        .def(nb::self != nb::self)
        .def(nb::self * double())
        .def(double() * nb::self)
        .def(nb::self / double())
        .def(nb::self *= double())
        .def(nb::self /= double())
        .def("__add__", [](trivec4d const& a, vec4d const& b) { return a + b; }, nb::is_operator())
        .def("__sub__", [](trivec4d const& a, vec4d const& b) { return a - b; }, nb::is_operator())
        ;
}
