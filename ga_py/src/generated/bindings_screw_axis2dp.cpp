// AUTO-GENERATED BY ga_bindgen — DO NOT EDIT.
// Regenerate via: python3 ga_bindgen/src/emit_nanobind.py --all
// Source manifest: ga_bindgen/manifest.json

#include <array>
#include <fmt/format.h>
#include <nanobind/nanobind.h>
#include <nanobind/ndarray.h>
#include <nanobind/operators.h>
#include <nanobind/stl/array.h>
#include <nanobind/stl/pair.h>
#include <nanobind/stl/string.h>
#include <nanobind/stl/string_view.h>
#include <nanobind/stl/vector.h>
#include <stdexcept>
#include <string>
#include <string_view>
#include <utility>
#include <vector>

#include "ga/ga_cga.hpp"
#include "ga/ga_ega.hpp"
#include "ga/ga_pga.hpp"
#include "ga/ga_sta.hpp"

namespace nb = nanobind;
using namespace hd::ga;
using namespace hd::ga::ega;
using namespace hd::ga::pga;
using namespace hd::ga::cga;
using namespace hd::ga::sta;

void bind_screw_axis2dp(nb::module_& m)
{
    nb::class_<screw_axis2dp>(m, "screw_axis2dp")
        .def("__init__", [](screw_axis2dp* self) { new (self) screw_axis2dp{}; })
        .def(
            "__init__",
            [](screw_axis2dp* self, vec2dp const& centre, double angle,
               vec2dp const& translation) {
                new (self) screw_axis2dp(centre, angle, translation);
            },
            nb::arg("centre"), nb::arg("angle"), nb::arg("translation"))
        .def_rw("centre", &screw_axis2dp::centre)
        .def_rw("angle", &screw_axis2dp::angle)
        .def_rw("translation", &screw_axis2dp::translation)
        .def("__repr__", [](const screw_axis2dp& v) { return fmt::format("{}", v); })
        .def("__str__", [](const screw_axis2dp& v) { return fmt::format("{}", v); })
        .def(
            "__format__",
            [](const screw_axis2dp& v, std::string_view spec) {
                try {
                    if (spec.empty()) return fmt::format("{}", v);
                    return fmt::format(fmt::runtime("{:" + std::string(spec) + "}"), v);
                }
                catch (fmt::format_error const& e) {
                    throw std::invalid_argument(e.what());
                }
            },
            nb::arg("format_spec"))
        .def("__eq__", [](const screw_axis2dp& a, const screw_axis2dp& b) {
            return a.centre == b.centre && a.angle == b.angle &&
                   a.translation == b.translation;
        });
}
