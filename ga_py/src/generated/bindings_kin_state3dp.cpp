// AUTO-GENERATED BY ga_bindgen — DO NOT EDIT.
// Regenerate via: python3 ga_bindgen/src/emit_nanobind.py --all
// Source manifest: ga_bindgen/manifest.json

#include <array>
#include <fmt/format.h>
#include <nanobind/nanobind.h>
#include <nanobind/ndarray.h>
#include <nanobind/operators.h>
#include <nanobind/stl/array.h>
#include <nanobind/stl/pair.h>
#include <nanobind/stl/string.h>
#include <nanobind/stl/string_view.h>
#include <nanobind/stl/vector.h>
#include <stdexcept>
#include <string>
#include <string_view>
#include <utility>
#include <vector>

#include "ga/ga_ega.hpp"
#include "ga/ga_pga.hpp"
#include "ga/ga_sta.hpp"

namespace nb = nanobind;
using namespace hd::ga;
using namespace hd::ga::ega;
using namespace hd::ga::pga;
using namespace hd::ga::sta;

void bind_kin_state3dp(nb::module_& m)
{
    nb::class_<kin_state3dp>(m, "kin_state3dp")
        .def("__init__", [](kin_state3dp* self) { new (self) kin_state3dp{}; })
        .def(
            "__init__",
            [](kin_state3dp* self, vec3dp const& vel, vec3dp const& acc,
               vec3dp const& omega,
               vec3dp const& alpha) { new (self) kin_state3dp(vel, acc, omega, alpha); },
            nb::arg("vel"), nb::arg("acc"), nb::arg("omega"), nb::arg("alpha"))
        .def_rw("vel", &kin_state3dp::vel)
        .def_rw("acc", &kin_state3dp::acc)
        .def_rw("omega", &kin_state3dp::omega)
        .def_rw("alpha", &kin_state3dp::alpha)
        .def("__repr__", [](const kin_state3dp& v) { return fmt::format("{}", v); })
        .def("__str__", [](const kin_state3dp& v) { return fmt::format("{}", v); })
        .def(
            "__format__",
            [](const kin_state3dp& v, std::string_view spec) {
                try {
                    if (spec.empty()) return fmt::format("{}", v);
                    return fmt::format(fmt::runtime("{:" + std::string(spec) + "}"), v);
                }
                catch (fmt::format_error const& e) {
                    throw std::invalid_argument(e.what());
                }
            },
            nb::arg("format_spec"))
        .def("__eq__", [](const kin_state3dp& a, const kin_state3dp& b) {
            return a.vel == b.vel && a.acc == b.acc && a.omega == b.omega &&
                   a.alpha == b.alpha;
        });
}
