// AUTO-GENERATED BY ga_bindgen — DO NOT EDIT.
// Regenerate via: python3 ga_bindgen/src/emit_nanobind.py --all
// Source manifest: ga_bindgen/manifest.json

#include <nanobind/nanobind.h>
#include <nanobind/ndarray.h>
#include <nanobind/operators.h>
#include <nanobind/stl/array.h>
#include <nanobind/stl/pair.h>
#include <nanobind/stl/string.h>
#include <nanobind/stl/string_view.h>
#include <nanobind/stl/vector.h>
#include <fmt/format.h>
#include <array>
#include <stdexcept>
#include <string>
#include <string_view>
#include <utility>
#include <vector>

#include "ga/ga_ega.hpp"
#include "ga/ga_pga.hpp"

namespace nb = nanobind;
using namespace hd::ga;
using namespace hd::ga::ega;
using namespace hd::ga::pga;

void bind_vector3d(nb::module_& m) {
    nb::class_<vector3d, vec3d>(m, "vector3d")
        .def(nb::init<>())
        .def(nb::init<double, double, double>())
        .def(nb::init<vec3d>())
        .def("__init__",
            [](vector3d* self,
               nb::ndarray<double, nb::shape<3>, nb::c_contig,
                           nb::device::cpu> arr) {
                double const* d = arr.data();
                new (self) vector3d(d[0], d[1], d[2]);
            })
        .def("__repr__", [](const vector3d& v) {
            return fmt::format("vector3d({}, {}, {})", v.x, v.y, v.z);
        })
        .def("__str__", [](const vector3d& v) {
            return fmt::format("{}", static_cast<const vec3d&>(v));
        })
        .def("__format__",
            [](const vector3d& v, std::string_view spec) {
                try {
                    if (spec.empty()) return fmt::format("{}", static_cast<const vec3d&>(v));
                    return fmt::format(fmt::runtime("{:" + std::string(spec) + "}"),
                                       static_cast<const vec3d&>(v));
                } catch (fmt::format_error const& e) {
                    throw std::invalid_argument(e.what());
                }
            }, nb::arg("format_spec"))
        .def("__array__",
            [](vector3d const& v, nb::handle /*dtype*/,
               nb::handle /*copy*/) {
                auto* data = new double[3]{v.x, v.y, v.z};
                nb::capsule owner(data, [](void* p) noexcept {
                    delete[] static_cast<double*>(p);
                });
                return nb::ndarray<nb::numpy, double, nb::shape<3>>(
                    data, { 3 }, owner);
            },
            nb::arg("dtype").none() = nb::none(),
            nb::arg("copy").none() = nb::none())
        ;
}
