// AUTO-GENERATED BY ga_bindgen — DO NOT EDIT.
// Regenerate via: python3 ga_bindgen/src/emit_nanobind.py --all
// Source manifest: ga_bindgen/manifest.json

#include <array>
#include <fmt/format.h>
#include <nanobind/nanobind.h>
#include <nanobind/ndarray.h>
#include <nanobind/operators.h>
#include <nanobind/stl/array.h>
#include <nanobind/stl/pair.h>
#include <nanobind/stl/string.h>
#include <nanobind/stl/string_view.h>
#include <nanobind/stl/vector.h>
#include <stdexcept>
#include <string>
#include <string_view>
#include <utility>
#include <vector>

#include "ga/ga_cga.hpp"
#include "ga/ga_ega.hpp"
#include "ga/ga_pga.hpp"
#include "ga/ga_sta.hpp"

namespace nb = nanobind;
using namespace hd::ga;
using namespace hd::ga::ega;
using namespace hd::ga::pga;
using namespace hd::ga::cga;
using namespace hd::ga::sta;

void bind_joint_drive2dp(nb::module_& m)
{
    nb::class_<joint_drive2dp>(m, "joint_drive2dp")
        .def("__init__", [](joint_drive2dp* self) { new (self) joint_drive2dp{}; })
        .def(
            "__init__",
            [](joint_drive2dp* self, double tau_max, double qd_max, double qdd_max,
               double armature, bool const& actuated) {
                new (self) joint_drive2dp(tau_max, qd_max, qdd_max, armature, actuated);
            },
            nb::arg("tau_max"), nb::arg("qd_max"), nb::arg("qdd_max"),
            nb::arg("armature"), nb::arg("actuated"))
        .def_rw("tau_max", &joint_drive2dp::tau_max)
        .def_rw("qd_max", &joint_drive2dp::qd_max)
        .def_rw("qdd_max", &joint_drive2dp::qdd_max)
        .def_rw("armature", &joint_drive2dp::armature)
        .def_rw("actuated", &joint_drive2dp::actuated)
        .def("__repr__", [](const joint_drive2dp& v) { return fmt::format("{}", v); })
        .def("__str__", [](const joint_drive2dp& v) { return fmt::format("{}", v); })
        .def(
            "__format__",
            [](const joint_drive2dp& v, std::string_view spec) {
                try {
                    if (spec.empty()) return fmt::format("{}", v);
                    return fmt::format(fmt::runtime("{:" + std::string(spec) + "}"), v);
                }
                catch (fmt::format_error const& e) {
                    throw std::invalid_argument(e.what());
                }
            },
            nb::arg("format_spec"))
        .def("__eq__", [](const joint_drive2dp& a, const joint_drive2dp& b) {
            return a.tau_max == b.tau_max && a.qd_max == b.qd_max &&
                   a.qdd_max == b.qdd_max && a.armature == b.armature &&
                   a.actuated == b.actuated;
        });
}
