// AUTO-GENERATED BY ga_bindgen — DO NOT EDIT.
// Regenerate via: python3 ga_bindgen/src/emit_nanobind.py --all
// Source manifest: ga_bindgen/manifest.json

#include <array>
#include <fmt/format.h>
#include <nanobind/nanobind.h>
#include <nanobind/ndarray.h>
#include <nanobind/operators.h>
#include <nanobind/stl/array.h>
#include <nanobind/stl/pair.h>
#include <nanobind/stl/string.h>
#include <nanobind/stl/string_view.h>
#include <nanobind/stl/vector.h>
#include <stdexcept>
#include <string>
#include <string_view>
#include <utility>
#include <vector>

#include "ga/ga_cga.hpp"
#include "ga/ga_ega.hpp"
#include "ga/ga_pga.hpp"
#include "ga/ga_sta.hpp"

namespace nb = nanobind;
using namespace hd::ga;
using namespace hd::ga::ega;
using namespace hd::ga::pga;
using namespace hd::ga::cga;
using namespace hd::ga::sta;

void bind_un_frame(nb::module_& m)
{
    nb::class_<un_frame>(m, "un_frame")
        .def("__init__", [](un_frame* self) { new (self) un_frame{}; })
        .def(
            "__init__",
            [](un_frame* self, vec2dp const& up, vec2dp const& north) {
                new (self) un_frame(up, north);
            },
            nb::arg("up"), nb::arg("north"))
        .def_rw("up", &un_frame::up)
        .def_rw("north", &un_frame::north)
        .def("__repr__", [](const un_frame& v) { return fmt::format("{}", v); })
        .def("__str__", [](const un_frame& v) { return fmt::format("{}", v); })
        .def(
            "__format__",
            [](const un_frame& v, std::string_view spec) {
                try {
                    if (spec.empty()) return fmt::format("{}", v);
                    return fmt::format(fmt::runtime("{:" + std::string(spec) + "}"), v);
                }
                catch (fmt::format_error const& e) {
                    throw std::invalid_argument(e.what());
                }
            },
            nb::arg("format_spec"))
        .def("__eq__", [](const un_frame& a, const un_frame& b) {
            return a.up == b.up && a.north == b.north;
        });
}
